include svg

block variables
- let mainTitle = "Cotime";

doctype html
html( lang="ru" )
  head
    meta( charset="UTF-8" )
    title #{mainTitle} !{title && "| " + title}
    meta( name="viewport" content="width=device-width, initial-scale=1.0" )
    link( rel="preload" href="fonts/MuseoSansCyrl-300.woff2" as="font" type="font/woff2" crossorigin )
    link( rel="preload" href="fonts/MuseoSansCyrl-500.woff2" as="font" type="font/woff2" crossorigin )
    link( rel="preload" href="fonts/MuseoSansCyrl-700.woff2" as="font" type="font/woff2" crossorigin )
    link( rel="stylesheet" href="css/style.min.css" )

  body
    header.header( class=(isEvent ? "is-event" : null) class=(isLanding ? "is-landing" : null) )
      .header__logo
        img(src="img/logo.svg", alt="logo", width="108", height="20")
      .header__links( class=(isEvent ? "hide" : null) )
        a.header__link(href="./prices") Тарифы
        a.header__link(href="./faq") Вопросы и ответы
        a.header__link(href="./new-event") + Создать занятие
      .header__account.header__account--logged
        a.header__link.header__link--enter(href="") Войти
        button.header__account--name( type="button" ) Maksimova M.
      .header__kebab


    main
      block content

    footer.footer( class=(isEvent ? "is-event" : null) )
      .footer__inner
        .footer__top
          .footer__logo © Cotime
          .footer__links
            a.footer__link(href="./prices") Тарифы
            a.footer__link(href="./faq") Вопросы и ответы
            a.footer__link(href="./documents") Документы
            a.footer__link.footer__link--email(href="mailto:hi@cotime.io")
              +svg("letter", "19", "15")
              | hi@cotime.io
            button.footer__link(type="button") En
        .footer__note
          | Мы используем cookies для сбора обезличенных персональных данных. Они помогают настраивать рекламу и анализировать трафик.<br> Оставаясь на сайте, вы соглашаетесь на сбор таких данных.


    block scripts
      script( src="js/script.min.js" )
