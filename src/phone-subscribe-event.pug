extends pug/layout.pug

block variables
  - let title = 'Введите номер телефона';
  - let isEvent = true;

block content
  .container.container--event
    form.phone-form.phone-form--subscribe(method="post")
      h1 #{title}
      input#phone( type="tel" placeholder="+7" )
      button.blue-btn(type="submit" disabled) Далее